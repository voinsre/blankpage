# =============================================================================
# BLANK PAGE WORTH — Environment Variables
# =============================================================================
# Copy this file to .env.local for local development.
# In production (Vercel), set these in the Vercel Dashboard > Settings > Environment Variables.
# NEVER commit .env.local to version control.
# =============================================================================


# -----------------------------------------------------------------------------
# SITE
# -----------------------------------------------------------------------------

# Your site URL (no trailing slash)
# Local: http://localhost:3000
# Production: https://blankpageworth.com
NEXT_PUBLIC_SITE_URL=http://localhost:3000


# -----------------------------------------------------------------------------
# ANTHROPIC (Claude AI)
# -----------------------------------------------------------------------------

# Your Anthropic API key
# Get it from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Primary model — Claude 3.5 Haiku (fast, cheap, good enough for single questions)
# If you get a 403 "model not available" error, your API plan may not include this model.
# In that case, the code will automatically fall back to ANTHROPIC_MODEL_FALLBACK.
ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# Fallback model — Claude 3 Haiku (older, universally available on all plans)
ANTHROPIC_MODEL_FALLBACK=claude-3-haiku-20240307


# -----------------------------------------------------------------------------
# SUPABASE
# -----------------------------------------------------------------------------

# Your Supabase project URL
# Get it from: Supabase Dashboard > Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co

# Your Supabase anonymous/public key (safe for client-side)
# Get it from: Supabase Dashboard > Settings > API > anon/public key
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Supabase service role key (server-side ONLY — never expose to client)
# Get it from: Supabase Dashboard > Settings > API > service_role key
# Used for: webhook handlers that need to update profiles without user auth
SUPABASE_SERVICE_ROLE_KEY=eyxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# -----------------------------------------------------------------------------
# STRIPE
# -----------------------------------------------------------------------------

# Your Stripe secret key (server-side ONLY)
# Get it from: Stripe Dashboard > Developers > API Keys > Secret key
# Use test key (sk_test_...) for development, live key (sk_live_...) for production
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe webhook signing secret
# Get it from: Stripe Dashboard > Developers > Webhooks > Select endpoint > Signing secret
# For local testing, use Stripe CLI: stripe listen --forward-to localhost:3000/api/webhook
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Stripe Payment Link URL
# Create in Stripe Dashboard:
#   1. Go to Products > "The Page"
#   2. Create a price: $2/month recurring with $22.95 setup fee
#   3. Create a payment link from that price
#   4. Set success URL to: https://blankpageworth.com/page?welcome=true
#   5. Copy the payment link URL here
NEXT_PUBLIC_STRIPE_PAYMENT_LINK=https://buy.stripe.com/xxxxxxxxxxxxx


# -----------------------------------------------------------------------------
# ANALYTICS (Optional)
# -----------------------------------------------------------------------------

# Plausible Analytics domain (if using Plausible)
# Sign up at: https://plausible.io or self-host
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=blankpageworth.com

# Umami Analytics (if using Umami instead of Plausible)
# NEXT_PUBLIC_UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# NEXT_PUBLIC_UMAMI_URL=https://your-umami-instance.com
